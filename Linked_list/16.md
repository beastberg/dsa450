### check if circular linked list
```cpp
bool isCircular(Node* head){
    if(!head)
        return false;
    Node* curr = head;
    while(curr->next){
        if(curr->next==head)
            return head;
        curr = curr->next;
    }
    return false;
}
```


